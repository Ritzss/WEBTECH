//!=============================strings=========================================
//string is a collection of charaters which are enclosed in single quotes(''), double quotes(""), back ticks(``)

//display string

var s='javascript';
console.log(s);
var s="javascript";
console.log(s);
var s=`javascript`;
console.log(s);

// ?string interpolation
// It is the process of acessing variable into strings.
// to use this------------> `${variableName}`
//examples

var sname = 'Ritzs';
var designation = 'world concurrer';
var print = `${sname} + 'is the' +${designation}`;

// 
